"""API version 1 URL configuration.

Endpoints (auth, apartments, preferences, recommendations, messaging,
verification, admin) are implemented in later phases. This module currently
has no routes so that empty endpoint definitions are not mistaken for working
functionality.
"""
from django.urls import path

urlpatterns = []
